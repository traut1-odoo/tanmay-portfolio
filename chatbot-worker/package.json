{
  "name": "tanmay-portfolio-chatbot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:context": "tsx scripts/build-context.mjs",
    "dev": "npm run build:context && wrangler dev",
    "deploy": "npm run build:context && wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.99.0"
  }
}
